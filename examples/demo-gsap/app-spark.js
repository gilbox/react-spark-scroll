module.exports = require('react-spark-scroll/spark-scroll-gsap')({
  invalidateAutomatically: true

  // we could add additional options in here...

  // setup: {...}
  // formulas: {...}
  // actionProps: {...}
});
