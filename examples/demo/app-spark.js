module.exports = require('react-spark-scroll/spark-scroll-rekapi')({
  invalidateAutomatically: true

  // we could add additional options in here...

  // setup: {...}
  // formulas: {...}
  // actionProps: {...}
});
